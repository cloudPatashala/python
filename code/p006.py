# Today we look at the python Length function.

# We will be using the input function to get the name of the person and then
# calculate the number of characters in it.

print(len(input("What is your first name: ")))

print(len(input("What is your Last name: ")))
